{
	"scripts": {
		"ci:build": "pnpm run -r build",
		"ci:publish": "pnpm run ci:build && pnpm changeset publish",
		"ci:version": "pnpm changeset version && pnpm install --lockfile-only"
	},
	"devDependencies": {
		"@changesets/cli": "^2.26.2",
		"prettier": "^2.8.8"
	}
}